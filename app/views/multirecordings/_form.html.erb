<%= form_with(model: multirecording) do |form| %>
  <% if multirecording.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(multirecording.errors.count, "error") %> prohibited this multirecording from being saved:</h2>

      <ul>
        <% multirecording.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
